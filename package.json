{
  "name": "node",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "babel ./server/component.js --watch -o ./server/component-compiled.js",
    "bundle": "webpack --watch",
    "server": "nodemon index.js --exec babel-node",
     "start": "start npm run server && start npm run bundle"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pug": "^2.0.0-beta11"
  }
}
